# 2026-03-15

- Updated `senku-ishigami` instance config to use `ollama/kimi-k2.5:cloud` and confirmed the `openclaw@senku-ishigami.service` restarted active.
- Verified `provisioning-architect` instance config declares primary model `ollama/glm-5:cloud`, but live self-reports still returned `ollama/kimi-k2.5:cloud`.
- Traced Provisioning Architect runtime split: live service uses `/root/.openclaw/instances/provisioning-architect.json` with state dir `/root/.openclaw/state-provisioning-architect`, while a separate legacy session tree under `/root/.openclaw/agents/provisioning-architect/` still answers model checks as Kimi.
- Found Provisioning Architect config validation failures from invalid auth profile keys (`auth.profiles.ollama:default.key` and `auth.profiles.vercel-ai-gateway:default.key`); removed those keys from the instance config.
- Durable lesson: model verification for per-agent runtimes should prefer the live state dir / active service session source over stale legacy session namespaces, or reports may incorrectly show old models.
