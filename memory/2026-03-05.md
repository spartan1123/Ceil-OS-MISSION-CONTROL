# 2026-03-05

- User provided exact Discord bot tokens for 11 Ceil agents and requested strict 1:1 assignment per agent (not reference-only storage).
- Updated `/root/.openclaw/instances/<agent>.json` for all listed agents so `channels.discord.token` matches user-provided token exactly.
- Verified mapping integrity across all 11 agents (`ALL_OK`).
- User confirmed restart succeeded and reported Discord is enabled/connected as `@Ceil`.
- `openclaw status --all` showed Discord channel ON/OK for active runtime.
- User validated behavior by seeing both `workspace-orchestrator` and `provisioning-architect` respond in Discord.
- Added new Discord agent `ariana` with dedicated bot token and dedicated channel `1477040738071220279`.
- Created `/root/.openclaw/instances/ariana.json` + `ariana.env` and added main binding in `/root/.openclaw/openclaw.json`.
- Debugged Ariana non-response: fixed startup command usage (`openclaw gateway run`) and resolved missing auth store by copying `auth-profiles.json` + `models.json` into `/root/.openclaw/state-ariana/agents/main/agent/`.
- Confirmed Ariana gateway listening on `:19111`, Discord login as `@Ariana Emails`, and successful replies in target channel.
- Configured `research-search` provider secret file at `agents/research-search/.secrets/web_search.env` with multi-provider failover policy and strict monthly cap rules (Brave $5 max; others free-tier only).
