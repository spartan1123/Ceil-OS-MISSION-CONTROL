## 2026-03-05 Council + Dashboard durable notes
- User preference: keep dashboard implementation lightweight and centered on a single editable UI file at `/root/.openclaw/workspace/ceil-workspace-dashboard/index.html` for feature work.
- User requested backups before major edits; backups were created with timestamped names in the same folder (e.g., `index.backup-2026-03-05-0522.html`, `index.backup-2026-03-05-1123.html`).
- Council execution was advanced from archive-only to run-oriented behavior in `index.html`:
  - Start Run / Stop Run / Sync actions in Council detail.
  - Run lifecycle states: `queued`, `running`, `completed`, `failed`, `stopped`.
  - Session run fields used: `last_run_id`, `run_status`, `run_started_at`, `run_finished_at`.
  - Local fallback storage keys: `council_runs:<run_id>`, `council_messages:<run_id>`, plus unsynced tracking.
- Runtime dispatch contract for Council is based on OpenClaw `/tools/invoke` with payload `{tool, action?, args?, sessionKey?, dryRun?}`.
- Browser token exposure was removed by adding a local server-side proxy endpoint:
  - `POST /api/tools/invoke` on the dashboard server.
  - Proxy reads gateway token server-side from `/root/.openclaw/openclaw.json` and forwards to `http://127.0.0.1:18789/tools/invoke`.
- Dashboard systemd service now runs a custom server script instead of `python -m http.server`:
  - Service file: `/etc/systemd/system/ceil-workspace-dashboard.service`
  - Server script: `/root/.openclaw/workspace/ceil-workspace-dashboard/dashboard_server.py`
  - Bind: `127.0.0.1:45680` (local-only) with strict origin checks for `/api/tools/invoke`.
- OpenClaw tool exposure update performed to support Council dispatch over HTTP `/tools/invoke`:
  - Added `gateway.tools.allow: ["sessions_send", "sessions_spawn"]`
  - Updated in:
    - `/root/.openclaw/openclaw.json`
    - `/root/.openclaw/instances/senku-ishigami.json`
- Verification snapshot:
  - `/api/tools/invoke` via dashboard proxy returned HTTP 200 for `sessions_list`.
  - Capability probe via proxy returned success for `sessions_send` and `sessions_spawn` dry runs after tool exposure update.
  - CORS behavior for proxy: allowed matching dashboard origin; denied mismatched origin.
- Data-layer caveat observed during verification: Supabase tables `council_sessions`, `council_runs`, and `council_messages` were not present in schema cache at check time (HTTP 404), so Council relies on local fallback until tables are created.
