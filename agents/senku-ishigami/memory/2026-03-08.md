- date: 2026-03-08
- task: Stabilize Ceil Workspace Dashboard + agent responsiveness + Supabase sync accuracy during Discord troubleshooting.
- assumptions:
  - Dashboard and agent instance configs are source of truth for runtime behavior.
  - Supabase tables (`agent_logs`, `todos`, `council_sessions`, `council_runs`, `council_messages`) use UUID ids and can reject legacy local IDs.
  - Per-agent instance configs under `/root/.openclaw/instances/*.json` drive live gateways.
- implementation decisions:
  - Increased dashboard proxy upstream timeout in `ceil-workspace-dashboard/dashboard_server.py` from 60s to 300s and restarted `ceil-workspace-dashboard.service`.
  - Switched agent defaults to faster Gemini Flash model (`google-gemini-cli/gemini-2.0-flash`) and set `agents.defaults.timeoutSeconds=300` across instance configs.
  - Added missing Gemini auth profile (`google-gemini-cli:default` with oauth + email) across instance/global configs to resolve widespread `HTTP 401: User not found`.
  - Expanded Council participant catalog in dashboard UI to include `os-monitor-template`, `research-search`, and `ariana` (plus slug mappings).
  - Fixed council ID/sync defects by replacing legacy `local-*` identifiers with UUID generation, adding UUID validation helpers, and migrating legacy local session ids during sync before Supabase upserts.
  - Added council session deletion UX: list/detail delete buttons, local+remote artifact cleanup, and replaced browser `window.confirm` with custom in-app delete modal.
  - Improved Agents panel data quality by filtering housekeeping/noise logs from "Tasks today" and preferring meaningful latest tasks for cards.
- verification evidence:
  - Confirmed modified code blocks in `index.html` and `dashboard_server.py` after edits.
  - Verified relevant services active with systemd status checks (`openclaw@senku-ishigami`, `openclaw@workspace-manager`, `ceil-workspace-dashboard`).
  - Observed runtime logs indicating Discord login and gateway service health after restarts.
- unresolved risks:
  - Mission Control totals for weekly metrics still partly raw-log based; may still diverge from "meaningful-only" interpretation in some widgets.
  - If Supabase RLS policies are re-enabled without matching policies, dashboard writes/reads can regress.
  - Historical local artifacts created before UUID migration may still require one-time resync/open-refresh per session.
- follow-up actions:
  - Align weekly totals/success-rate widgets to same meaningful-task filter logic.
  - Add explicit UI toggle for "Raw logs" vs "Meaningful tasks".
  - Add end-to-end validation path for council create/run/sync/delete against Supabase with policy checks.
